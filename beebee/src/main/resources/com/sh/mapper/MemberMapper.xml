<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.sh.mapper.MemberMapper">
	
	
	<!-- 세션을 이용해서 만들려고 했던 회원 정보 -> 시큐리티로 변경
	<select id="selectList" resultType="com.sh.domain.MemberVO">
		select email, nickname, pwd
		from tbl_members
	</select>
	
	<select id="getByEmail" parameterType="String" resultType="com.sh.domain.MemberVO">
		select * from tbl_members where email = #{email}
	</select>

	<insert id="insert">
		insert into tbl_members (email, nickname, pwd)
		values (#{email}, #{nickName}, #{pwd})
	</insert>
	
	<select id="emailCheck" parameterType="String" resultType="int">
		select COUNT(email) from tbl_members where email = #{email}
	</select>

	<select id="nickNameCheck" parameterType="String" resultType="int">
		select COUNT(nickName) from tbl_members where nickname = #{nickname}
	</select>
	
	<select id="login" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM tbl_members WHERE email = #{email} AND pwd = #{pwd}
	</select>
	 -->
</mapper>